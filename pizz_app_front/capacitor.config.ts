import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.deliveryapp',
  appName: 'Pizz App Delivery',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
