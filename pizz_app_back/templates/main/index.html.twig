{% extends 'base.html.twig' %}

{% block title %}Orders page{% endblock %}

{% block body %}

{% include('navbar/navbar.html.twig') %}

    <div class="container">

        <h5 class="text-center mt-5">COMMANDES NON AFFECTEES</h5>

        <div style="margin: 0 auto;" class="row">
        {% for order in orders %}

            <div class="col-4">
                <div class="card m-3" style="width: 18rem;">

                    <div class="card-body">
                    <h5 class="card-title">Commande n°: {{ order.reference }}</h5>

                        {#href="{{ path('order.html.twig', {'id' : order.id }) }}"#}
                        <a style="background-color: green;" class="btn mb-2" href="{{ path('order_details', {'id' : order.id }) }}">Details</a>
                        <a href="{{ path('app_main_delete_by_id', {'id' : order.id }) }}"  style="background-color: green;" class="btn mb-2">Supprimer la commande</a>

{#                        <a  href="{{ path(' app_main_edit_order') }}" style="background-color: green;" class="btn mb-2">Affecter</a>#}
                        <a href="{{ path('app_main_affect_order', {'id' : order.id}) }}"  style="background-color: green;" class="btn mb-2">Affecter</a>
                    </div>
                </div>
            </div>
        {% endfor %}


        </div>
    </div>
{% endblock %}
